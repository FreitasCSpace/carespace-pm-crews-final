confirm_post:
  description: >
    The sprint digest has already been posted to Slack by the system.
    Check digest_posted in your inputs — it should be "true".
    Return a confirmation: "Sprint digest posted for [digest_summary]."
    Do NOT call any tools. Do NOT post anything.
  expected_output: >
    Confirmation that the digest was posted.
  agent: daily_pulse_agent
