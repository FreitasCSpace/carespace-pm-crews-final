confirm_task:
  description: >
    The sprint operation was handled by the system. Check sprint_result
    in your inputs and return it as confirmation. Do NOT call any tools.
  expected_output: >
    The sprint_result message from inputs.
  agent: sprint_agent
